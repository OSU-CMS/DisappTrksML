Bootstrap: docker
From: tensorflow/tensorflow:2.6.1-gpu-jupyter

%environment
    PATH=/usr/local/bin:$PATH
    # Needed to use legacy optimizers

%post
    pip install cmsml numpy optuna

%runscript
    python runNN.py $1